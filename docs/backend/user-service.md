# User-service

The user service is responsible for managing users and authentication.

It connects to the database (PostgreSql) and provides REST API for user management and authentication.

It also provides JWT authentication and authorization.