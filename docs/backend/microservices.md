# Microservices

The backend is split into microservices. Each microservice is a separate Spring Boot application.

## Common

Common library containing code and utilities shared between microservices.

## User-service

The user service responsible for managing users and authentication.